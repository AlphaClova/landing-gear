# Vitest loads this over .env so unit tests stay on fixtures and do not call the live backend.
VITE_CHAT_API_MODE=mock
VITE_USE_MOCK_API=true
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_CHAT_TIMEOUT_MS=30000
